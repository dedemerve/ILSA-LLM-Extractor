Patch the following ILSA extraction JSON for Excel export completeness.
Apply SYSTEM rules strictly. Return ONLY the full corrected JSON.

CORPUS CONTEXT (do not invent beyond this record):
- Corpus: 1,266 unique articles after DOI/title deduplication from 1,680 collected studies
  (IEA n=308, OECD n=591, Scopus n=423, Web of Science n=302, Google Scholar n=56).

CURRENT JSON:
```json
{PASTE_FULL_ARTICLE_JSON_HERE}
```

Self-check before returning:
- weight_fields_interpretation >= 3 sentences (from existing fields only)
- sample_filtering_criteria non-empty
- all enums are legal literals
- no "N/A" in variable_code
- handling_not_reported_explanation if PV/missing is not_reported/not_applicable
- null_fields_interpretation if >3 critical fields still null
- zero information added outside ABSOLUTE RULE 1 sources

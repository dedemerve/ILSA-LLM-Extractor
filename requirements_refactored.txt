# ILSA Pipeline Refactored (v2.0) - Python Dependencies
# Install with: pip install -r requirements_refactored.txt

# Core dependencies
pydantic==2.7.0                    # Strict type validation with Optional fields
pymupdf==1.24.2                    # PyMuPDF - efficient PDF text extraction

# LLM API clients (install only what you need)
google-generativeai==0.3.0         # For Gemini 2.5-Flash (recommended for nano)
# openai==1.12.0                    # Uncomment for OpenAI API (gpt-4o-mini fallback)

# Utilities
python-dotenv==1.0.0               # Load environment variables from .env

# Optional: Database and logging
sqlalchemy==2.0.28                 # For database storage (optional)
# structlog==24.1.0                 # Structured logging (optional)

# Development dependencies (optional)
# pytest==7.4.4                     # Testing framework
# black==24.1.1                     # Code formatting
# ruff==0.3.2                       # Linting
